PORT=3000
NODE_ENV=development

# Database Configuration
# Local development (default)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/hotdrop
# Docker development (uncomment if running backend via docker-compose)
# DATABASE_URL=postgres://postgres:postgres@postgres:5432/hotdrop

# Postgres Image Env Vars (for Docker)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=hotdrop

# Authentication (Better Auth)
BETTER_AUTH_SECRET=changeme_dev_secret_123456
BETTER_AUTH_URL=http://localhost:3000
