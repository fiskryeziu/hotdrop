# Vite environment variables for production
# These will be embedded in the build

# API Base URL - update this for production deployment
VITE_API_URL=http://localhost:3000

# Socket.IO URL - update this for production deployment
VITE_SOCKET_URL=http://localhost:3000
